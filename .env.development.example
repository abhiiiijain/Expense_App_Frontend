# Copy to .env.development:
#   cp .env.development.example .env.development

# Dev / preview server port
VITE_PORT=4043

# Backend origin only (no /api/v1 — that is added in code)
VITE_API_BASE_URL=http://localhost:4044
